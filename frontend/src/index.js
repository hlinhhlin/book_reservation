// index.js

import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Import the App component

ReactDOM.render(<App />, document.getElementById('root')); // Assuming you have a div with the id 'root' in your HTML
